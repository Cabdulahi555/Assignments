/*******************************************************************
Cabdulahi Hussein
Homework 8 - reverseNum.cpp
This  program that will ask for a non-negative integer number (zero or positive value) and show all the digits backwards. 
******************************************************************/
#include <iostream>
using namespace std;

int main()
{ // initialize numbers
  int num;
  int remainder;
  int reverse = 0;

  
  // ask for input
  cout <<"Enter a non-negative number: ";
  cin >> num;

  // while loop to check if num if negative
  while (num < 0)
{

      cout <<"The number is invalid. Enter a number again:";
}
  // process to find and output the remainders
  while(num >  0)
{  
      remainder = num % 10; // gets remainder of number
      reverse = reverse * 10 + remainder; // for placement, adds it in correct order
      num = num / 10;
      cout <<remainder;
}

  return 0;
}
