// For the lab final EC, the header file is for the 4 grocery lines
// The class for the content of each slot of the array must be #included

// Your name:
//-----------------------------------------------

#include "lqueue.h"
using namespace std;

class grocery
{
 private:
  lqueue G[4];

 // complete the rest based on the assignment sheet

  public:
    // Constructor

  class InvalidLine{};
  class Underflow{};
    grocery();
  ~grocery();  
    // PURPOSE: Check if a specific line is empty
    bool isEmpty(int line);
    
    // PURPOSE: Add a customer to the shortest line
    void addRear(string newElem, int lineNumber);
    
    // PURPOSE: Serve (remove) the front customer from a specific line
    void deleteFront(int lineNumber);
    
    // PURPOSE: Display all 4 lines
    void displayAll();

    //Purpose: Displays just one line contents
    void displayLine(int lineNumber);

};
